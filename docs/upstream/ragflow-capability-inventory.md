# RAGFlow 能力提取清单

## 后端优先评估目录

- `api/`: 可复用的服务入口和接口模式
- `rag/`: 检索、召回、重排相关能力
- `deepdoc/`: 文档解析能力，后续只保留非 OCR 路径
- `agent/`: 工作流与 Agent 能力
- `web/`: 仅用于参考信息架构，不直接复用页面

## 明确删除范围

- 用户、登录、认证、租户、组织、成员、权限
- OCR 模型、OCR 配置、OCR 任务链路、OCR 入口
